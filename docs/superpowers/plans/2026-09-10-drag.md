# 奶蛙提起与惯性摆动实施计划

**Goal:** 头顶尖端固定在鼠标上，身体绕尖端随拖动惯性摆动，停手收稳。

**Architecture:** PetWindow 统一构造绘制变换，在抓取点旋转并进行鼠标位置补偿。独立的 DragSway 按秒计算速度和阻尼弹簧，不依赖鼠标事件频率。松手保持身体中心在屏幕上的位置，再回正。

**Tech Stack:** Python、PySide6、pytest。

- [x] 在 src/drag.py 实现可独立测试的摆动模型，限制角度并分步积分避免长帧发散。
- [x] 在 src/config.py 校准头顶抓取点、速度增益、弹簧与阻尼。
- [x] 在 src/pet_window.py 统一绘制变换；按鼠标锁定抓取点，定时器推进惯性，松手补偿身体中心。
- [x] 添加 tests/test_drag.py，检查方向、反向、停手衰减、帧率稳定性、Qt 鼠标事件和双朝向锚点约束。
- [x] 运行 pytest 与离屏窗口渲染检查；构建本地可执行文件供用户体验。

用户已确认交互方案；本次本地实施，不提交、不上传。

验证：拖拽专项 4 项通过；其他不依赖桌面命名管道的回归 23 项通过。IPC 测试遇到命名管道占用（231），未停止用户进程。原生桌面鼠标手感仍需用户体验，Qt 事件及离屏绘制已验证。
