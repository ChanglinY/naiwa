# 坐压摧毁动画实现计划

**Goal:** 按用户指定的五段素材播放，并在销毁2结束时回收文件。
**Architecture:** 新增动画资产与五段状态机，分别发出坐压完成和全片完成事件；会话消费回收事件且保持到收尾完成。
**Tech Stack:** Python / PySide6、OpenCV、Pillow、Windows 回收站 API。

- [x] 检查源素材，生成对齐透明帧与坐压锚点、动作预览。
- [x] 配置五段动画，修改状态机与会话的回收时点、定位和中断处理。
- [x] 添加关键时序测试，运行实际 Qt 动画与临时文件回收验证。
- [ ] 更新说明和交接记录，重启本地奶蛙供用户使用。

不提交或推送，不操作用户现有文件的回收。

## 验证与最终回身方案

用户确认：第四段由水平镜像正放改为销毁1倒放、不镜像，优先动作衔接。121 帧已按原片反序烘焙，运行时无需再次 reverse。起身→回身端点图像平均差异 0.00236，回身→闻手 0.00069。

- 五段共 509 帧、24fps，坐压锚点在 assets/destroy.json。
- 单元测试 35 passed、2 skipped。
- 实际 Qt 点击验证：错误点击保持确认，正确点击进入追逐；完整动作顺序正确。
- 临时桌面文件实际回收验证通过：销毁2完成、销毁3第0帧时调用一次回收；收尾继续播放，接触点误差小于1px。最新结果 build/destroy-flow-result.json，渲染预览 build/destroy-flow.gif。
- 没有操作用户既有文件。
